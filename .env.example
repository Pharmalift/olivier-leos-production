# ================================
# SUPABASE CONFIGURATION
# ================================

# URL de votre projet Supabase (Public)
# Trouvez-le dans: Supabase Dashboard > Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Clé publique Supabase (Public - Safe to expose)
# Trouvez-la dans: Supabase Dashboard > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Clé Service Role Supabase (PRIVÉ - NE JAMAIS EXPOSER CÔTÉ CLIENT!)
# Trouvez-la dans: Supabase Dashboard > Settings > API > Project API keys > service_role
# ⚠️ Cette clé contourne TOUTES les règles RLS - À utiliser UNIQUEMENT côté serveur
# Utilisée par: scripts/create-admin.ts
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ================================
# SMTP EMAIL CONFIGURATION (Gmail)
# ================================

# Serveur SMTP Gmail
SMTP_HOST=smtp.gmail.com

# Port SMTP (587 pour STARTTLS)
SMTP_PORT=587

# Adresse email d'envoi (expéditeur des emails)
# Doit être un compte Gmail avec "Mot de passe d'application" activé
# Instructions: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com

# Mot de passe d'application Gmail (PAS le mot de passe habituel!)
# Format: xxxx xxxx xxxx xxxx (4 groupes de 4 caractères)
# Créer un mot de passe d'application: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=xxxx xxxx xxxx xxxx

# Email de l'administrateur (reçoit les notifications de commandes)
ADMIN_EMAIL=admin@example.com

# ================================
# NOTES IMPORTANTES
# ================================

# 1. NE JAMAIS commiter le fichier .env.local dans Git
# 2. Sur Vercel, configurez ces variables dans: Settings > Environment Variables
# 3. Les variables NEXT_PUBLIC_* sont accessibles côté client (browser)
# 4. Les autres variables sont UNIQUEMENT accessibles côté serveur
# 5. Pour Gmail: Activez "Accès moins sécurisé" ou utilisez un mot de passe d'application
